{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Attach to running Go process",
          "type": "go",
          "request": "attach",
          "mode": "local",
          "processId": "${command:pickProcess}",
      }
  ]
}
